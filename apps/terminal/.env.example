# Google OAuth "Desktop app" client, embedded into dist/cli.js at build time (see README).
# Build-time env vars of the same name win over this file; at runtime they replace the embedded pair.
MYSTICALS_GOOGLE_CLIENT_ID=
MYSTICALS_GOOGLE_CLIENT_SECRET=
