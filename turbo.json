{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": { "inputs": ["$TURBO_DEFAULT$", "$TURBO_ROOT$/.env"], "outputs": ["out/**"], "env": ["MYSTICALS_GOOGLE_*"] },
    "mysticals#build": { "inputs": ["$TURBO_DEFAULT$", "$TURBO_ROOT$/.env"], "outputs": ["dist/**"], "env": ["MYSTICALS_GOOGLE_*"] },
    "typecheck": {},
    "test": {},
    "e2e": { "dependsOn": ["build"], "cache": false, "passThroughEnv": ["MYSTICALS_MOCK"] },
    "dist": { "dependsOn": ["build"], "cache": false },
    "dev": { "cache": false, "persistent": true, "passThroughEnv": ["MYSTICALS_MOCK"] }
  }
}
